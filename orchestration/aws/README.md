# AWS Orchestration

**Status: NOT IMPLEMENTED.**

## Future purpose

Will hold the AWS pipeline/orchestration definitions that replicate the
Azure orchestration (see [../azure/README.md](../azure/README.md)) once the
AWS port ticket (RF-011) begins. This is a deliberate later port, not a
parallel build — see [DECISIONS.md](../../DECISIONS.md) #003.

## Learning objectives

- Comparing AWS orchestration primitives against the Azure equivalent
  already built
- Porting a working pipeline design to a different cloud provider's
  services
- Identifying what is genuinely provider-specific vs. portable in a data
  pipeline design

No deployment instructions or credentials are provided here — none exist
yet. See [docs/cloud_mapping.md](../../docs/cloud_mapping.md) for the
planned service mapping.
